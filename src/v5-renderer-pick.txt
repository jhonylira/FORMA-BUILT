pickFloor(px,py){if(!latest||!project)return null;let chosen=null,best=-Infinity;const p=latest.p,ya=p.yaw*Math.PI/180,pi=p.pitch*Math.PI/180;const depth=a=>(a[0]*Math.sin(ya)+a[2]*Math.cos(ya))*Math.cos(pi)+a[1]*Math.sin(pi);
function triangle(a,b,c,key){const A=project(a),B=project(b),C=project(c),den=(B[1]-C[1])*(A[0]-C[0])+(C[0]-B[0])*(A[1]-C[1]);if(Math.abs(den)<1e-8)return;const u=((B[1]-C[1])*(px-C[0])+(C[0]-B[0])*(py-C[1]))/den,v=((C[1]-A[1])*(px-C[0])+(A[0]-C[0])*(py-C[1]))/den,w=1-u-v;if(Math.min(u,v,w)<-1e-6)return;const z=u*depth(a)+v*depth(b)+w*depth(c);if(z>best){best=z;chosen=key;}}
for(const b of latest.boxes){if(b.type==='crown'||p.viewMode==='envelope'||p.floorIsolate&&b.floor>Number(p.floorFocus.split(':')[0]))continue;const key=FloorDesign.key(b),ps=b.polys||LotGeometry.rect(b);for(const poly of ps){const flat=earcut.flatten(poly),indices=earcut(flat.vertices,flat.holes,2);for(let i=0;i<indices.length;i+=3){const pts=indices.slice(i,i+3).map(j=>[flat.vertices[j*2],b.y+b.h,flat.vertices[j*2+1]]);triangle(...pts,key);}for(const r of poly)for(let i=1;i<r.length;i++){const a=r[i-1],q=r[i],A=[a[0],b.y,a[1]],B=[q[0],b.y,q[1]],C=[q[0],b.y+b.h,q[1]],D=[a[0],b.y+b.h,a[1]];triangle(A,B,C,key);triangle(A,C,D,key);}}}return chosen;},
